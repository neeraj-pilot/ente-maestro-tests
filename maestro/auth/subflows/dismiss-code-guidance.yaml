appId: ${APP_ID}
---
- extendedWaitUntil:
    visible: ".*${READY_ACCOUNT}.*"
    timeout: 30000
- runFlow:
    when:
      visible:
        id: auth_code_coach_mark_dismiss
    commands:
      - tapOn:
          id: auth_code_coach_mark_dismiss
